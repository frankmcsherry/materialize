{
  "name": "mz-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "MVP: consistent multi-view replication from Materialize to Postgres via SUBSCRIBE AS OF + RETAIN HISTORY",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
